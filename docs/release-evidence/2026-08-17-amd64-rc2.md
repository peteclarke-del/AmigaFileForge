# AMD64 RC2 release evidence, 17 August 2026

This record covers local validation of `1.0.0-rc.2` at merged commit `4e9c2e8`.
The older RC1 record remains in this directory because it documents a distinct
build and test population.

## Environment

- Host architecture: `x86_64`
- Runtime: rebuilt `amiga-file-forge:latest` Docker image
- Final image identifier: `sha256:3dbabda782b3a097912d05885d4c63688c48b5fbd400ed45dcdb1ecdfca110b2`
- Application version: `1.0.0-rc.2`
- Persistent data: existing `amiga-file-forge-work` volume retained

## Complete Python suite

Command:

```sh
python -m unittest discover -s tests -v
```

Result: 481 tests completed in 351.260 seconds. There were no failures; 10
tests requiring optional private fixtures were skipped.

## JavaScript and browser suites

JavaScript syntax and regression checks passed. The following Playwright suites
also completed without failure against the rebuilt service:

- editor menu behaviour;
- movable workspace windows;
- generated HDF, checkpoints, undo, operations and save packaging;
- workspace search, image comparison and guarded patch preflight;
- browser-private collection catalogue;
- cheat analysis and guarded patch workflow.

`git diff --check` also passed for the validated tree.

## Scope and remaining gates

This evidence establishes the local AMD64 automated result for RC2. It does not
claim an independent clean build on ARM64 or ARMv7. It also does not replace the
manual Amiga, Amiga 600, Hardfile, FastFileSystem or AmigaOS hardware checks in the release
checklist. Those gates remain open until their own environment details, commands
and results are retained.
